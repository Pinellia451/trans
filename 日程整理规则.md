# 日程整理规则

## 1. 日历归属

- **默认写入 `calender/事务.ics`**：会议、预约、截止日期、提醒、临时安排、个人事项等，除非用户明确指定其他日历。
- **仅在明确涉及课程表、课表、上课安排或用户指定时**，才更新 `calender/18周课表_2026秋季.ics` 或其他课表文件。
- 不因事项内容看起来像学习相关，就自动写入课表；以用户的明确意图为准。

## 2. 信息整理原则

- 适当合并原始表述，使日历标题、地点和描述适合日历展示。
- 不擅自改变用户未要求修改的时间、日期、重复周期、链接、地点、人员或状态。
- 时间统一使用中国标准时间（`Asia/Shanghai`），并明确开始时间和结束时间。
- 需要线上参加的事项，在 `LOCATION` 中标记为 `线上｜平台名称`；线下事项保留建筑和房间信息。
- 重要的链接、会议号、联系人、教师、备注放入 `DESCRIPTION`，信息分行记录。
- 会议类日程标题优先采用“平台｜主题或发起人”格式；避免重复、冗余的原始文案。

## 3. 重复日程规则

- **规律性重复的事项，优先使用 `RRULE`，不要为每周创建大量独立事件。**
- `RRULE` 至少包含：`FREQ`、必要时的 `INTERVAL`、`BYDAY` 和结束日期 `UNTIL`。
- `UNTIL` 使用 UTC 格式，并将本地最后一次发生时间按中国标准时间换算为 UTC。
- 每个具有不同星期、时间、地点、教师或其他关键属性的规律，建立独立的重复事件。
- 双周、隔周安排使用 `INTERVAL=2`；单周/双周教师不同但其他信息相同的课程，优先拆为两条对应的重复系列，确保每次显示的教师准确。
- 不连续但总体规律的安排：以重复规则覆盖基础周期，再用 `EXDATE` 排除取消或不发生的日期；如例外日期改为补课或其他安排，应另建事件。
- 若同一事项的时间、地点或关键内容发生阶段性变化，应拆分为多个重复系列，不用一条规则强行覆盖。
- 重复规则的起始日使用真实的第一次发生日期，不使用任意占位日期。

## 4. 事件字段规范

- 每个事件必须有稳定且唯一的 `UID`；修改已有事件时尽量保留原 `UID`。
- `DTSTAMP` 记录本次生成或修改时间。
- 设置：`STATUS:CONFIRMED`、`TRANSP:OPAQUE`，除非用户另有要求。
- 默认添加提前 15 分钟的 `VALARM`，提醒描述使用“[标题]将在15分钟后开始”。
- 会议链接同时放在 `URL`（如适用）和描述中，会议号单独列出。
- ICS 文本使用规范的 CRLF 换行；描述中的换行使用 `\\n` 表示，并注意特殊字符转义。

## 5. 修改、校验与交付流程

1. 先读取远程仓库当前版本，确认目标文件和已有事件，避免覆盖他人更新。
2. 根据上述归属规则确定写入事务日历还是课表；默认选择事务日历。
3. 修改后校验 `BEGIN:VEVENT` 与 `END:VEVENT` 数量一致，所有事件均有有效的开始/结束时间；检查重复规则、时区、例外日期和会议链接。
4. 检查日历文本格式及 `git diff --check`，确认没有意外改动。
5. 提交后推送到远程 `main` 分支。
6. 向用户输出：变更摘要、目标文件、重复规则/例外、提交或推送结果；若推送失败，明确说明失败原因，不宣称已同步。

## 6. 默认决策

当用户只提供一条新日程而未说明日历归属时：

> 写入 `calender/事务.ics`，按上述信息整理；只有用户明确说“课程表/课表/上课”或指定课表文件时，才写入课表。
